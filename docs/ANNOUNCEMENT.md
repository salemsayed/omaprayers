# Announcement copy

Image to attach: `docs/images/announcement.png` (1600×790, 2:1 — renders full-width
in an X timeline without cropping).

## Main tweet

> OmaPrayers — prayer times for the Omarchy bar.
>
> Most widgets answer "when is Maghrib". The more useful question is "how much of
> this window is left" — so the panel draws the day to scale, with a needle on now.
>
> Arabic and English. Offline cache. Any theme.
>
> github.com/salemsayed/omaprayers

265 characters plus the link. Swap "Omarchy" for the project's @handle if you
want the mention.

## Shorter variant

> OmaPrayers — prayer times for the Omarchy bar.
>
> The whole day drawn to scale, with a needle on now. Arabic and English,
> offline cache, works with any theme.
>
> github.com/salemsayed/omaprayers

## Optional follow-up (thread)

> Two layouts behind one setting: Horizon draws the day to scale and adds a
> window-length column. Compact is the ruled timetable card — dot leaders, three
> tones for past / current / next.
>
> Both inherit your theme. No bundled palette, no bundled font.

> Arabic isn't an afterthought: names, dates and countdown units are localised,
> the layout mirrors properly, and text renders in Noto Naskh Arabic rather than
> whatever the monospace bar font falls back to.

## Notes

- Publish `github.com/salemsayed/omaprayers` before using either announcement
  variant; the repository URL is not live yet.
- Do not claim mosque iqama accuracy. The README is deliberately careful here and
  the announcement should match: these are calculated times, method-dependent.
- If you post before the marketplace listing is approved, link the repo rather
  than omarchyplugins.com.
